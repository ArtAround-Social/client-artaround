
const initialState = {
  invites: [
    { id: 1, name: 'MarkovCo Gallery' },
    { id: 12, name: 'Dragon Fire' },
    { id: 13, name: 'Basic Space' },
    { id: 13, name: 'Basic sgggSpace' }
  ]
};

export default function reducer(state = initialState, action) {
  switch(action.type) {
    default:
      
      return state;
  }
}
